<div> 
		<h5> <%= @artist.name %> </h5>
		<p> age:  <%= @artist.age %></p>
		<p> bio: <%= @artist.bio %></p>
		<%= link_to "Edit Artist", edit_artist_path(@artist)%>
		<%= link_to "Delete Artist", delete_artist_path(@artist)%>
</div>
